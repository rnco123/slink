# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts and git hooks must stay LF — CRLF breaks `sh` execution.
*.sh text eol=lf
.husky/pre-commit text eol=lf
.husky/pre-push text eol=lf
