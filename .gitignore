# dependencies
node_modules/
.pnpm-store/

# builds
.next/
.medusa/
dist/
build/
out/

# turbo
.turbo/

# env — never commit secrets
.env
.env.*
!.env.template
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# misc
.DS_Store
Thumbs.db
*.pem
coverage/

# e2e / Playwright test output (regenerated each run)
apps/storefront/e2e/.artifacts/
test-results/
playwright-report/

# IDE
.vscode/*
!.vscode/extensions.json
.idea/

# terraform
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
!*.auto.tfvars.example

# local-only living tracker (contains credentials/notes) — NEVER commit.
# NOTE: this file was tracked in early history (commit cd27a52); it has been
# `git rm --cached`ed since. History must be scrubbed of it before the first
# push to a public repo (roadmap task 16).
completion.md
